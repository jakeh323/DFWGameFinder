{
  "name": "dfw-game-finder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp -r public/* dist/"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.0",
    "openai": "^6.0.0"
  }
}